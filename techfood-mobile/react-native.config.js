module.exports = {
    project: {
        ios: {},
        android: {}, // grouped into "project"
    },
    assets: ['./Main/assets/fonts/'], // stays the same
    commands: [], // formerly "plugin", returns an array of commands
};